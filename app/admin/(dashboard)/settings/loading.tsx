import { SettingsPageSkeleton } from "@/components/admin/Skeleton";

/** 設定配下の遷移スケルトン。見出しとタブ（settings/layout.tsx）は残る */
export default function SettingsLoading() {
  return <SettingsPageSkeleton />;
}
