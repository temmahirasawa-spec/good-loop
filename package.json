{
  "name": "good-loop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "next lint --max-warnings 0",
    "check": "npm run typecheck && npm run lint && npm run secrets && npm run tenant && npm run design && npm run build",
    "secrets": "node scripts/check-secrets.mjs",
    "tenant": "node scripts/check-tenant.mjs",
    "design": "node scripts/check-design-tokens.mjs",
    "design:figma": "node scripts/check-figma.mjs",
    "screenshot": "node scripts/screenshot.mjs"
  },
  "dependencies": {
    "next": "14.2.35",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.35",
    "playwright": "^1.62.1",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
