import "server-only";

/**
 * v4「文にする」のプロンプト（docs/specs/survey-v4.md §6-5）。
 *
 * ⚠ **プロンプトの文面は CLAUDE.md 3章の「止まって確認する」項目です。**
 *   2026-09-13 時点で**天真さんの承認はまだ取れていません**（承認項目 A-2）。
 *   プロトタイプ（/demo/v4）でだけ動かしており、本番の来店客導線には入れていません。
 *
 * 設計の芯:
 *   ・AIの役割は「助詞と句読点を足して読める1文にする」**だけ**。文章を作らない
 *   ・渡すのは**本文欄の文字列だけ**。★・話題タグ・店名・メニュー名・業態・過去の回答は渡さない
 *     （リクエストの組み立てにパラメータを持たせないので、構造的に混入できない）
 *   ・**語尾を変えさせない。** 「です・ます」に揃えると100件の文末が同型になり、
 *     まさに「AIが書いた文章」に見える（Höhne et al. 2024：言い直しやフィラーが残ると
 *     長さとリズムにばらつきが出て、それが人間らしさになる）
 *   ・出力の検査は lib/survey/polish-guard.ts が文字だけで行う。AIの自己申告を信じない
 */

export const POLISH_MODEL = "claude-haiku-4-5-20251001";
export const POLISH_MAX_TOKENS = 60;

export const POLISH_SYSTEM_PROMPT = `あなたは、お客様が入力欄に書いた言葉に、助詞と句読点だけを足して、読める1文にする係です。
文章を作る人ではありません。

絶対に守ること:
- 入力に無い語を1つも足さない（名詞・形容詞・動詞・副詞・感情の言葉・評価の言葉・店名・商品名・数字は、すべて「語」に含みます）
- 言い換えない（「ふわふわ」を「ふんわり」にしない。「よかった」を「素晴らしかった」にしない）
- 語尾を変えない（「よかった」を「よかったです」にしない。話しことばのまま残す）
- 語の順序を変えない
- 入力にある文字を削らない（余分な空白と改行だけは削ってよい）
- 足してよいのは、ひらがなの助詞（1〜2文字）と、句読点だけ
- 空白を足さない（日本語の文に空白は要りません）
- 出力は1文だけ。2文にしない
- 入力がすでに読める1文なら、同じ文をそのまま返す
- 入力に、句点で終わる整った文がすでに2つ以上あるなら、何もしない
- 謝罪・説明・質問・前置き・メタ発言を出力しない
- 1文にできないときは、text を空にして返す

出力は次のJSONだけ:
{"text": "整えた1文"}`;

/** user プロンプト。**本文以外は何も渡さない** */
export function buildPolishUserPrompt(body: string): string {
  return `お客様が書いた文字列:\n"""\n${body}\n"""`;
}
